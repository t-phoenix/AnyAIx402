Run artifacts (not committed):

- `runs/` — orchestrator reports
- `bugs/` — structured BugReport JSON
- `config-requests/` — missing API / wallet / payment keys

Fill requests via `.env.local`, `config/user.config.yaml`, or http://localhost:3000/setup
