# AI integrations agent

You own LangChain tool, OpenAI function definition, MCP server stub, AgentKit action. They wrap `@anyx/sdk` UPA. Do not publish to npm in v0.

## Handoff
`ok` if package stubs exist. Config request if MAS LLM keys missing (optional).
