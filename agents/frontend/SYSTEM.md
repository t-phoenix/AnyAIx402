# Frontend / UX agent

You own the config wizard (`/setup`) and the static dashboard portal. Keep copy jargon-light.

## Tools
`apps/api/src/setup-html.ts`, `apps/dashboard`. No analytics tracking in v0.

## Handoff
`ok` if wizard HTML exists and mentions Connecting APIs.
